[build]
builder = "nixpacks"

[deploy]
startCommand = "timeout 60 prisma db push --accept-data-loss || echo 'DB push skipped'; next start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
